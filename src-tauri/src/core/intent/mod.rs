//! # intent — 意图与能力判定模块
//!
//! 只保留"规则层 + 能力冲突快速判定"这两件现在真正被使用的东西：
//!
//! - `rules`: 关键词规则表。产出粗粒度意图（CHAT / QUESTION / ACTION / TASK_PLAN / …），
//!   当前只被"复杂任务 → 走方案审批流程"这一处使用。
//! - `plan_detector`: 正文中的"计划内容"检测（Edit/Plan 模式下的后置拦截）。
//!
//! ## 已删除（第三步清理）
//!
//! - 三层意图分类链（规则 → 上下文 → 轻量模型兜底）与 `CapabilityRequirement`：
//!   它此前只服务于"只读保护（chat）模式"，该模式与它一并被移除。
//! - 能力冲突快速判定（`mode_conflict`）：规划模式下"用户要改文件"不再提前截断，
//!   交给模型自己解释并走方案审批流程（用户少一次往返）；能力边界仍通过上下文声明 +
//!   工具目录过滤 + 运行时校验三层保证。
//! - 连带删除 `pipeline::validate()`：DANGEROUS / UNCLEAR 两个分支都已不可达
//!   （危险操作改由"执行前权限判定"兜住，看的是真实命令与路径，比看用户措辞更准）。

pub mod plan_detector;
pub mod rules;
